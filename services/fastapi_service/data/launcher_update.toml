# Tauri 自更新配置
# 由 /v1/r5/launcher/update/{target}/{arch}/{current_version} 接口读取并返回

# 当前最新版本号（若未在下方 [[versions]] 中找到，则自动 fallback 到第一个条目）
latest = "0.14.0"

# 版本列表，新版本追加在前面
# platforms key 格式: {target}-{arch}，如 windows-x86_64, linux-x86_64, darwin-aarch64

[[versions]]
version = "0.13.0"
notes = "初始版本"
pub_date = "2026-04-13T15:00:00+08:00"

[versions.platforms.windows-x86_64]
signature = ""
url = "https://hk.gh-proxy.org/https://github.com/sleep1223/r5r-cn-launcher/releases/download/v0.13.0/R5R-CN-Launcher_0.13.0_x64-setup.exe"